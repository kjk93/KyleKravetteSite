<div class="col-xs-4">
	<div class="thumbnail">
		<%= image_tag picture.picture.url %>
		<div class="caption">
			<% if picture.description.blank? %>
				<p><%= picture.description %></p>
			<% end %>
			<p><strong><%= "Order: #{picture.order}" %></strong></p>
			<%= link_to "Edit", edit_picture_path(picture), class: "btn btn-primary" %>
		</div>
	</div>
</div>